package Office_Hours.Practicew_11_4_20;

import java.util.Scanner;

public class UniqueAndDuplicatesChars {
    public static void main(String[] args) {

       /* String str = "aabccd";
        String unique = "";//bd
        String duplicates = "";
for(int j=0; j<str.length();j++) { //finding frequency of all characters
    char ch = str.charAt(j);//'a','a, 'b,'c,'c,'d
    int frequency = 0;//frequency of ch
    for (int i = 0; i < str.length(); i++) { //for finding frequency of 1 character
        if (str.charAt(i) == ch) {
            frequency++;
        }
    }
    if (frequency == 1) {//if frequency is 1=> ch is unique
        unique += ch;
    }
    if(frequency > 1){//if more than 1, charcter is duplicated
        duplicates+=ch;
    }
}
        System.out.println(unique);
        System.out.println(duplicates);   */
        Scanner scan = new Scanner(System.in);
        System.out.println("Enter the String");
        String str = scan.next();



    }
}
